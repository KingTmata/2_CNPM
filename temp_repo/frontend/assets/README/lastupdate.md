Data chi tiết sản phẩm thêm vào chi tiết trong dtb
Mai sau thay đoạn tk mẫu thành đăng ký tài khoản, lấy data giống thay đổi thông tin tài khoản cài đặt tài khoản - lưu dtb data custom
Chưa có kiểm tra tương thích- rule base
"Hệ thống sử dụng mô hình Client-Server với RESTful API. Frontend (HTML/JS) gọi API qua `fetch()` đến ASP.NET Core backend. Backend xử lý theo layered architecture: Controller → Service → Repository → Entity Framework → SQL Server. Dữ liệu được backend trả về dưới dạng JSON, frontend chỉ có nhiệm vụ hiển thị và gửi request. Đây là kiến trúc chuẩn cho ứng dụng web hiện đại."
 trang thanh toán đang sai, 